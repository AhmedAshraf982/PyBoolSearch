Entry Point OF The Project is main.py
Run main.py
In src folder:
    BOOLEAN MODEL contain inverted_index and positional index
    Query It contain And, Or, Not and proxmity queries function
    which give result set of containing document id
    in Utils it only preprocess:
        remove stopwords
        remove special characters
        lower case
        and append into list
        in a tuple format

IN GUI enter query AND , OR ,NOT in this format

click search after enter the query

and it result will appear in the textlist below the Result